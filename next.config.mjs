

const nextConfig = {
  reactStrictMode: true,
  output: 'export',          // generates static `out/` folder
  images: { unoptimized: true }, // required for static export
};

export default nextConfig;
